# -----------------------------------
#  Copyright IBM Corp. 2021. All rights reserved.
#  US Government Users Restricted Rights - Use, duplication or disclosure
#  restricted by GSA ADP Schedule Contract with IBM Corp.
# -----------------------------------

# LSF directories as they exist on your current lsf master(s)
LSF_SUITE_TOP: /changeme/opt/ibm/lsfsuite
LSF_TOP: "{{ LSF_SUITE_TOP }}/lsf"
LSF_CONF_DIR: "{{ LSF_TOP }}/conf"
LSF_WORK_DIR: "{{ LSF_TOP }}/work"
# Modify 10.1 if the version is different
LSF_VER_DIR: "{{ LSF_TOP }}/10.1"
# The jq binary needs to be available in LSF_GUI_BIN/
LSF_GUI_BIN: "{{ LSF_SUITE_TOP }}/ext/gui/3.0/bin"
LSF_LOG_DIR: /opt/ibm/lsflogs

# LSF on-cloud cluster name
LSF_CLUSTER_NAME: changeme-cloudonly

# LSF_TOP on the image you plan to deploy with resource connector
# may be different than the static cluster's
rc_image_LSF_TOP: /changeme/opt/ibm/lsf_worker

LSF_NFS_MNT_DIR: /changeme/mnt/nfs

